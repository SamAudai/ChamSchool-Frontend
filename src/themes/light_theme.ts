import { Theme } from '../themes/symbols';

export const lightTheme: Theme = {
  name: 'light',
  properties: {
    '--background': '#f6f7f9',
    '--on-background': '#000',
    '--primary': '#07456b',
    '--on-primary': '#000'
  }
};