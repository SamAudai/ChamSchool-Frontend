import { Theme } from '../themes/symbols';

export const darkTheme: Theme = {
  name: 'dark',
  properties: {
    '--background': '#000',
    '--on-background': '#fff',
    '--primary': '#292929',
    '--on-primary': '#07456b',
  }
} 